/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cloudmall-africa.herokuapp.com", "api.cloudmall.africa"],
  },
};

module.exports = nextConfig;
